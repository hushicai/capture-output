{
  "name": "capture-output",
  "version": "0.0.4",
  "description": "capture output of shell command",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "capture",
    "output",
    "shell"
  ],
  "bin": {
    "capture-output": "./bin/capture-output"
  },
  "author": "hushicai <bluthcy@gmail.com> (http://hushicai.com/)",
  "license": "ISC",
  "dependencies": {
    "webshot": "^0.15.2"
  }
}
